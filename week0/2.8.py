from math import fractions
def sortfractions(fractions):
    sortedfraction = []
    for n in fractions:
        fraction = fraction.Fraction(n)
        if fraction >= 
            fract.append(n)
        elif 
